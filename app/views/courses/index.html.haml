%h1 All Courses


%table#courses
  %body
    - @courses.each do |course|
    %tr
      %td= course.title
      %td= link_to "Course Discription", course_path(course)

=link_to 'Add new course', new_course_path
