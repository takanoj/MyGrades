%h1 All Courses


%table#courses
  %tr
    %td Course

  - @courses.each do |course|
    %tr
      %td= course.title
      %td= link_to "Course Information", course_path(course)
      %td= link_to "Edit Course", edit_course_path(course)
      %td= link_to "Delete Course", course_path(course),        |
          method: :delete, data: { confirm: 'Are you sure?' } |
= link_to 'Add new course', new_course_path


