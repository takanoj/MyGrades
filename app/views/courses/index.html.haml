%h1 All Courses


%table#courses
  %tr
    %td Course

  - @courses.each do |course|
    %tr
      %td= course.title
      %td= link_to "Course Details", course_path(course)

= link_to 'Add new course', new_course_path


